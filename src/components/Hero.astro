---
import FeaturedCard from '@components/FeaturedCard.astro';

const { featuredPost } = Astro.props;
---

<section class="w-full relative">
    <FeaturedCard featuredPost={featuredPost} />
</section>